interface:
  display_name: "Dsa Connect"
  short_description: "Repo-specific patterns and workflows for dsa-connect"
  default_prompt: "Use the dsa-connect repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true